# ذاكرة تطوير إيجنت استديو

## لماذا أضفنا هذا التنظيم؟

هذه النسخة مسؤوليتنا في الصيانة والاختبار. طلب صاحب المشروع استخدام
`Understand-Anything` لمساعدتنا على فهم الكود، **لا دمجه داخل التطبيق**.
الهدف حفظ ما تحققنا منه وأسباب القرارات، وتقليل إعادة تحليل المشروع كاملًا
واستهلاك الكريديت في كل جلسة. الخريطة وسيلة وصول إلى الكود وليست بديلًا عنه،
ولا نضمن رقمًا ثابتًا للتوفير أو أن تحليل الأداة خالٍ من الأخطاء.

## ترتيب القراءة في كل مهمة

قواعد تعاون الفريق والمساعدين في [قوانين الفريق](TEAM-RULES.md)، وسجلات
التسليم المنفصلة في [سجلات التغييرات](changes/README.md). لا تنقل كل سجل هنا؛
يبقى هذا الملف للمعرفة والقرارات الدائمة، وليس يوميات كل مطور.

1. اقرأ `AGENTS.md` و`docs/PERSONAL-FORK.md` للحواجز والرخصة ومسارات البيانات.
2. اقرأ هذه الوثيقة وسجل القرارات أدناه، ثم افحص `git status --short`.
3. إن وجدت `.ua/meta.json` وخريطة صالحة، قارن أساسها بتاريخ Git والتعديلات غير
   المثبتة. ابحث عن الملفات المتعلقة بالمهمة فقط؛ لا تحمّل الخريطة كلها للمحادثة.
4. افتح التنفيذ الحالي واختباراته. بعض أقسام `ARCHITECTURE.md` تصف بنية مستهدفة؛
   تحقق من وجود المسار فعلًا قبل نسب أي سلوك إليه.
5. عند التعديل، حدّث القرار المرتبط به والفحص المناسب، ثم حدّث الجزء المتأثر
   من الخريطة عند الحاجة. لا تعِد التحليل الشامل تلقائيًا لكل commit.

## فصل أداة التطوير عن المنتج

- مصدر الأداة خارج المستودع: `/home/twuijri/.understand-anything/repo`.
- المصدر المثبّت: `Egonex-AI/Understand-Anything`، commit
  `6df3065f1d8ddc2ce3615314d1d493f36d6b1c80`.
- مهارات المساعد مرتبطة بمجلدات المهارات في المصدر الخارجي، لتعمل مسارات
  السكربتات واعتمادياتها كما صممها المشروع. لا توجد حزمة للأداة في اعتماديات Studio.
- `.ua/` مكان إعداد نطاق التحليل ونتائجه القابلة لإعادة الاستخدام فقط؛ لا تُنسخ
  إلى صورة Docker. لا نضيف خادم لوحة الأداة إلى حاوية Studio أو شبكتها العامة.
- تثبيت الأداة لا يعني وجود خريطة مكتملة. النطاق الأول مقترح لمداخل المحادثة
  وتركيب تشغيل الوكلاء، ويحتاج تأكيد المستخدم قبل أول مسح حسب تعليمات المهارة.

## التحديث التدريجي والتحقق

شغّل مهارة `understand` عند الحاجة إلى بناء أو تحديث الخريطة. يجب أن تستعمل
سكربتات الأداة المعتمدة للمسح والبصمات؛ لا ننشئ بديلًا يدويًا لها.
احتفظ ببيانات `meta.json` و`fingerprints.json` وجرد الملفات الذي تعتمد عليه
النسخة المثبتة للتحديث التدريجي. التزم بأسماء الملفات الفعلية التي تنتجها الأداة.
إذا غاب الأساس أو تغير نطاق الاستبعاد فلا تدّعِ أن الخريطة حديثة أو شاملة.

استخدم `understand-chat` أو `understand-explain` للاسترجاع الموجّه، و`understand-diff`
لفهم أثر التغييرات. لا تفعّل hooks تشغّل تحليلًا مدفوعًا مع كل حفظ أو commit.
شغّل لوحة الأداة محليًا عند طلبها، ولا تنشرها على رابط مفتوح؛ قد تكشف تفاصيل الكود.

## الخصوصية

نحلل كود النسخة الشخصية فقط، وليس بيانات سيرفر المستخدم. استبعد ملفات البيئة
والأسرار والمفاتيح وقواعد البيانات والمحادثات ووحدات تخزين Hermes وStudio.
راجع `.ua/.understandignore` قبل توسيع النطاق. الكود الذي نرسله إلى مساعد التحليل
قد يُعالج لدى مزود النموذج؛ وجود الأداة محليًا لا يعني أن تحليلها اللغوي محلي.
راجع كل مخرجات المعرفة قبل رفعها، حتى لو كان المستودع خاصًا.

## سجل القرارات

اتجاه المحتوى مستقل عن لغة الواجهة: المرجع الملزم والأمثلة وحدود حواجز
الاختبارات في [قاعدة اتجاه النصوص](CONTENT-DIRECTION.md). لا تعِد ابتكار الحل
لكل شاشة ولا تغيّر نص المستخدم المخزن لإصلاح عرضه.

| القرار | السبب والحدود | موضع التنفيذ أو التوثيق |
| --- | --- | --- |
| اسم مؤقت: Agent Studio | إعادة تسمية الواجهة دون كسر معرّفات البروتوكولات ومسارات البيانات | `docs/PERSONAL-FORK.md` |
| حفظ الرخصة والنسب | لا نحذف الرخصة أو ننسب كود المطور لأنفسنا | `LICENSE`، `NOTICE.personal.md`، حارس الرخصة |
| إبقاء تاريخ مساهماتنا | الدمج السلوكي أهم من نسخ إصلاح قديم عوّضه المطور | `docs/personal-pr-inventory.json` |
| الاتصال المباشر بدل الخدمة الرسمية | استخدام سيرفرنا وحساباته؛ لا فتح اشتراك رسمي أو التحايل على استحقاقاته | لوحة الاتصال الشخصية وحواجز الربط الرسمي |
| تعطيل محدّث Studio الرسمي | يمنع استبدال نسختنا أو ضياع التخصيصات | مسار تحديث upstream المعزول |
| إزالة API Relay من التنقل | الزر رابط تسجيل بإحالة تجارية، وليس مكوّن تمرير الطلبات | `PageSidebarNav.vue`؛ لا حذف لمزودي المستخدم |
| الأداة خارج المنتج | تحسين فهمنا للكود دون زيادة اعتماديات التطبيق أو اتصالاته | هذه الوثيقة و`.ua/` |
| تحديثات المطور اختيارية ومراجعة | نراجع الرخصة والتعارضات والسلوك ولا نستبدل فرعنا كاملًا | `scripts/prepare-upstream-update.mjs` |
| بيانات التشغيل خارج الحاوية | إعادة إنشاء الحاوية يجب ألا تمحو حالة التطبيق والوكلاء | دليل الانتقال ووحدتا التخزين الحاليتان |
| توافق الستاك القديم بتغيير الصورة فقط | لا نطلب فوليومات جديدة أو تغيير الشبكة أو PATH؛ تثبيتات الوكلاء الجديدة داخل `/home/agent/.hermes/coding-agent/npm` | مدخل Docker منفصل يصلح PATH داخليًا ويبقي اكتشاف التثبيتات السابقة؛ لا تغيير لسطح المكتب أو منازل البروفايلات |
| تحديثات أمنية قبل أول نشر | لا يكفي نجاح الاختبارات إذا كانت حزم الإنتاج أو الحزم المضمّنة في الواجهة قديمة | `docs/PERSONAL-VALIDATION.md` وملفات قفل الاعتماديات |
| إصدار شخصي يدوي | ننشر صورة للكود المراجع ثم يختار المستخدم موعد تبديل ستاكه | `personal-image.yml`؛ لا نشر تلقائي لسيرفره |
| وسم latest بطلب المستخدم | اسم قصير يتبع آخر صورة منشورة اجتازت الفحوص؛ ليس آخر كود غير مختبر، ولا يحدّث الحاويات تلقائيًا | ترقية نفس البصمة في `personal-image.yml`، والسحب الصريح مع سياسة `missing` |
| منفذ الصورة الافتراضي 6060 | `EXPOSE` لا يضبط منفذ التطبيق؛ ضبط `PORT` يمنع أن يبدأ على 8648 بينما القالب يوجه إلى 6060 | `Dockerfile`؛ ستاك المستخدم السابق يحدد `PORT` بالفعل |
| لوحة الكانبان أعمدة أفقية بسحب وإفلات، لا لوحة رسم | طلب المالك تجربة شبيهة بـ Trello؛ لوحة Vue Flow (upstream #2296) لم تدعم سحب البطاقات. السحب بين الأعمدة يمر عبر أوامر Hermes CLI الموجودة فقط (`complete`/`block`/`unblock`/`archive` والمربوطة حديثًا `promote`/`schedule`/`request-review`/`reopen-review`)؛ عمودا `triage` و`running` لا يقبلان الإفلات. ترتيب البطاقات داخل العمود يُحفظ في متصفح المستخدم فقط لأن Hermes لا يملك حقلًا له؛ ترتيب الأعمدة ثابت وفق سير العمل ولا يُسحب (قرار المالك 2026-09-17) | `docs/changes/2026-09-15-twuijri-kanban-board-dnd.md`، `utils/hermes/kanban-board.ts`، اعتمادية `vue-draggable-plus` |

عند ظهور قرار جديد أضف السبب والحدود والدليل هنا، ولا تكتفِ بوصف «تم إصلاحه».
الاختبارات والنتائج الفعلية وحدود الجاهزية تسجّل في `docs/PERSONAL-VALIDATION.md`.
