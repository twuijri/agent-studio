# Shared project instructions

@AGENTS.md

Read `docs/TEAM-RULES.md` before changing files. It is the shared team policy,
not a separate Claude-specific ruleset. Keep this file short; update the shared
policy instead of duplicating it here. Do not rely on local auto-memory as the
team's only record of decisions or unfinished work.
