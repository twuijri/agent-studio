# المساهمة في إيجنت استديو

ابدأ من [قوانين الفريق](docs/TEAM-RULES.md). تنطبق على المطورين وعلى مساعديهم،
سواء استُخدم `Codex` أو `Claude Code` أو العمل اليدوي. صاحب التعديل البشري
مسؤول عن مراجعته، ولا تعني إجابة المساعد أن الكود صحيح أو آمن للنشر.

## بداية سريعة

1. اقرأ القوانين و[حدود النسخة والرخصة](docs/PERSONAL-FORK.md).
2. اتفق على المهمة والمسؤول ونطاق الملفات مع الفريق قبل بدء عمل متداخل.
3. اعمل على فرع مستقل من `origin/main`، وليس `upstream/main`.
4. أنشئ [سجل مهمة](docs/changes/README.md) وحدّثه أثناء العمل وعند التسليم.
5. استخدم [دليل التطوير](DEVELOPMENT.md) و[فحوص نوع التغيير](docs/harness/validation.md).
6. اعرض النتيجة والمخاطر والفحوص على صاحب الطلب؛ لا تفتح طلب دمج أو تنشر دون الإذن.

صاحب المشروع `twuijri` وحده يقرر وينفّذ الدمج إلى `main` بعد نجاح الفحوص
والتحقق من التوافق مع آخر `main`. المساهمون والمساعدون لا يدمجون ولا يفعّلون
الدمج التلقائي. راجع قيد حماية المستودع الحالي في قوانين الفريق قبل منح صلاحيات.

إعداد بيئة التطوير يستخدم `Node.js 24`:

```bash
npm ci --ignore-scripts
npm rebuild node-pty
npm run harness:check
npm run test:personal
```

ابدأ المساعد من جذر المستودع. اطلب منه تلخيص القيود التي قرأها قبل التعديل.
إذا لم تكن أداة المساعد تدعم ملفات تعليمات المشروع، مرّر لها القوانين يدويًا.
لا ترفع المفاتيح أو محادثات العملاء أو ملفات الذاكرة المحلية إلى المستودع.
